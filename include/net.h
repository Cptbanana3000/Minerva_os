#ifndef MINERVA_NET_H
#define MINERVA_NET_H

#include <stdint.h>

#define NET_HTTP_CAPTURE_SIZE 8192u
#define NET_HTTP_HOST_SIZE 48u
#define NET_HTTP_PATH_SIZE 64u
#define NET_X509_NAME_SIZE 32u
#define NET_TLS_P256_SIZE 32u
#define NET_TLS_KEX_PARAMS_SIZE 96u
#define NET_TLS_FINISHED_SIZE 12u

typedef struct {
    uint8_t local_ip[4];
    uint8_t gateway_ip[4];
    uint8_t dns_ip[4];
    uint8_t gateway_mac[6];
    uint8_t dns_mac[6];
    uint8_t gateway_mac_valid;
    uint8_t dns_mac_valid;
    uint8_t dns_last_ip_valid;
    uint8_t dns_last_ip[4];
    uint8_t dns_ip_count;
    uint8_t dns_ip_selected;
    uint8_t dns_result_ips[4][4];
    uint32_t arp_requests;
    uint32_t arp_replies;
    uint32_t dns_queries;
    uint32_t dns_replies;
    uint32_t rx_frames;
    uint16_t last_ethertype;
    uint16_t last_udp_src;
    uint16_t last_udp_dst;
    uint16_t last_dns_id;
    uint16_t last_dns_flags;
    uint32_t tcp_syns;
    uint32_t tcp_synacks;
    uint32_t tcp_acks;
    uint32_t tcp_errors;
    uint16_t last_tcp_src;
    uint16_t last_tcp_dst;
    uint8_t last_tcp_flags;
    uint32_t last_tcp_seq;
    uint32_t last_tcp_ack;
    uint8_t tcp_target_ip[4];
    uint16_t tcp_target_port;
    uint16_t tcp_local_port;
    uint32_t tcp_local_next;
    uint32_t tcp_remote_next;
    uint32_t tcp_payload_rx;
    uint32_t tcp_payload_tx;
    uint32_t tcp_payload_acked;
    uint32_t tcp_payload_send_errors;
    char http_host[NET_HTTP_HOST_SIZE];
    char http_path[NET_HTTP_PATH_SIZE];
    uint16_t http_request_len;
    uint16_t http_response_len;
    uint16_t http_body_offset;
    uint8_t http_stage;
    uint16_t http_status;
    uint8_t http_valid;
    uint8_t tls_stage;
    uint8_t tls_valid;
    uint16_t tls_client_hello_len;
    uint8_t tls_client_random[NET_TLS_P256_SIZE];
    uint8_t tls_server_random[NET_TLS_P256_SIZE];
    uint8_t tls_record_type;
    uint16_t tls_record_version;
    uint16_t tls_record_len;
    uint8_t tls_handshake_type;
    uint8_t tls_server_hello;
    uint16_t tls_server_version;
    uint16_t tls_cipher_suite;
    uint8_t tls_session_id_len;
    uint16_t tls_extensions_len;
    uint8_t tls_certificate;
    uint8_t tls_server_key_exchange;
    uint16_t tls_server_key_exchange_len;
    uint8_t tls_kex_curve_type;
    uint16_t tls_kex_named_curve;
    uint8_t tls_kex_pubkey_len;
    uint8_t tls_kex_pubkey_valid;
    uint32_t tls_kex_pubkey_x32;
    uint32_t tls_kex_pubkey_y32;
    uint8_t tls_kex_sig_hash;
    uint8_t tls_kex_sig_alg;
    uint16_t tls_kex_sig_len;
    uint8_t tls_kex_sig_r_len;
    uint8_t tls_kex_sig_s_len;
    uint32_t tls_kex_sig_r32;
    uint32_t tls_kex_sig_s32;
    uint8_t tls_kex_sig_r[NET_TLS_P256_SIZE];
    uint8_t tls_kex_sig_s[NET_TLS_P256_SIZE];
    uint16_t tls_kex_params_len;
    uint32_t tls_kex_params_hash32;
    uint8_t tls_kex_sig_verify_inputs;
    uint8_t tls_kex_sig_point_done;
    uint8_t tls_kex_sig_match;
    uint32_t tls_kex_sig_v32;
    uint8_t tls_client_key_exchange;
    uint8_t tls_client_key_exchange_acked;
    uint16_t tls_client_key_exchange_len;
    uint8_t tls_client_pubkey_valid;
    uint32_t tls_client_pubkey_x32;
    uint32_t tls_client_pubkey_y32;
    uint8_t tls_shared_secret_valid;
    uint32_t tls_shared_secret_x32;
    uint8_t tls_master_secret_valid;
    uint32_t tls_master_secret_0;
    uint32_t tls_master_secret_1;
    uint8_t tls_key_block_valid;
    uint16_t tls_key_block_len;
    uint32_t tls_client_write_key32;
    uint32_t tls_server_write_key32;
    uint32_t tls_client_write_iv32;
    uint32_t tls_server_write_iv32;
    uint16_t tls_transcript_len;
    uint8_t tls_transcript_overflow;
    uint32_t tls_handshake_hash32;
    uint8_t tls_client_finished_valid;
    uint32_t tls_client_finished0;
    uint32_t tls_client_finished1;
    uint32_t tls_client_finished2;
    uint8_t tls_ccs_tx;
    uint8_t tls_finished_tx;
    uint8_t tls_finished_acked;
    uint16_t tls_finished_record_len;
    uint32_t tls_finished_tag32;
    uint8_t tls_server_ccs_rx;
    uint8_t tls_server_finished_rx;
    uint8_t tls_server_finished_decrypt;
    uint8_t tls_server_finished_verify;
    uint32_t tls_server_finished0;
    uint32_t tls_server_finished1;
    uint32_t tls_server_finished2;
    uint32_t tls_server_finished_tag32;
    uint8_t tls_app_tx;
    uint8_t tls_app_acked;
    uint8_t tls_app_rx;
    uint8_t tls_app_decrypt;
    uint16_t tls_app_record_len;
    uint16_t tls_app_plain_len;
    uint16_t tls_app_response_len;
    uint16_t tls_app_pending_len;
    uint16_t tls_app_pending_rx;
    uint32_t tls_app_tag32;
    uint32_t tls_app_response_tag32;
    uint32_t tls_certificate_len;
    uint32_t tls_certificate_rx;
    uint32_t tls_certificate_list_len;
    uint32_t tls_first_certificate_len;
    uint32_t tls_second_certificate_len;
    uint8_t tls_first_certificate_der;
    uint8_t tls_first_certificate_der_header_len;
    uint32_t tls_first_certificate_der_len;
    uint8_t tls_second_certificate_der;
    uint8_t tls_chain_link;
    uint8_t tls_x509_tbs;
    uint32_t tls_x509_tbs_len;
    uint8_t tls_x509_serial_len;
    uint8_t tls_x509_sig_oid_len;
    uint8_t tls_x509_sig_alg;
    uint8_t tls_x509_outer_sig_oid_len;
    uint8_t tls_x509_outer_sig_alg;
    uint16_t tls_x509_signature_len;
    uint8_t tls_x509_signature_unused_bits;
    uint8_t tls_x509_signature_r_len;
    uint8_t tls_x509_signature_s_len;
    uint32_t tls_x509_signature_r32;
    uint32_t tls_x509_signature_s32;
    uint8_t tls_x509_signature_r[NET_TLS_P256_SIZE];
    uint8_t tls_x509_signature_s[NET_TLS_P256_SIZE];
    uint8_t tls_x509_tbs_hash_alg;
    uint32_t tls_x509_tbs_hash32;
    uint8_t tls_x509_tbs_hash[NET_TLS_P256_SIZE];
    uint8_t tls_x509_ecdsa_scalar_inputs;
    uint8_t tls_x509_ecdsa_pubkey_valid;
    uint8_t tls_x509_ecdsa_point_done;
    uint8_t tls_x509_ecdsa_match;
    uint32_t tls_x509_ecdsa_w32;
    uint32_t tls_x509_ecdsa_u1_32;
    uint32_t tls_x509_ecdsa_u2_32;
    uint32_t tls_x509_ecdsa_v32;
    uint8_t tls_x509_validity;
    uint8_t tls_x509_not_before_tag;
    uint8_t tls_x509_not_before_len;
    uint8_t tls_x509_not_after_tag;
    uint8_t tls_x509_not_after_len;
    uint32_t tls_x509_not_before_date;
    uint32_t tls_x509_not_after_date;
    uint8_t tls_x509_pubkey_oid_len;
    uint8_t tls_x509_pubkey_alg;
    uint8_t tls_x509_pubkey_curve;
    uint16_t tls_x509_pubkey_len;
    uint32_t tls_x509_pubkey_x32;
    uint32_t tls_x509_pubkey_y32;
    uint8_t tls_x509_pubkey_x[NET_TLS_P256_SIZE];
    uint8_t tls_x509_pubkey_y[NET_TLS_P256_SIZE];
    uint8_t tls_x509_issuer_cn_len;
    uint8_t tls_x509_subject_cn_len;
    char tls_x509_issuer_cn[NET_X509_NAME_SIZE];
    char tls_x509_subject_cn[NET_X509_NAME_SIZE];
    uint8_t tls_x509_chain_subject_cn_len;
    char tls_x509_chain_subject_cn[NET_X509_NAME_SIZE];
    uint8_t tls_x509_chain_pubkey_alg;
    uint8_t tls_x509_chain_curve;
    uint16_t tls_x509_chain_pubkey_len;
    uint32_t tls_x509_chain_pubkey_x32;
    uint32_t tls_x509_chain_pubkey_y32;
    uint8_t tls_x509_chain_pubkey_x[NET_TLS_P256_SIZE];
    uint8_t tls_x509_chain_pubkey_y[NET_TLS_P256_SIZE];
    uint8_t tls_x509_verify_inputs;
    uint8_t tls_x509_known_issuer;
    uint8_t tls_x509_san_dns_len;
    char tls_x509_san_dns[NET_X509_NAME_SIZE];
    uint8_t tls_x509_host_match;
    uint8_t tls_x509_basic_constraints;
    uint8_t tls_x509_is_ca;
    uint8_t tls_x509_key_usage;
    uint8_t tls_x509_key_usage_bits;
    uint8_t tls_x509_eku;
    uint8_t tls_x509_eku_bits;
    uint8_t tls_pending_handshake_type;
    uint32_t tls_pending_handshake_len;
    uint32_t tls_pending_handshake_rx;
    uint16_t tls_record_pending;
    uint8_t http_response[NET_HTTP_CAPTURE_SIZE];
} net_info_t;

void net_init(void);
const net_info_t *net_info(void);
int net_arp_probe_gateway(void);
int net_dns_query_example(void);
int net_tcp_connect_example(void);
int net_tcp_connect_dns(void);
int net_http_get_example(void);
int net_http_get_url(const char *url);
int net_https_get_url(const char *url);
int net_tls_probe_example(void);
int net_tls_probe_url(const char *url);
int net_tls_verify_signature(void);
int net_tls_verify_kex_signature(void);
int net_tls_send_client_key_exchange(void);
int net_tls_derive_keys(void);
int net_tls_compute_finished(void);
int net_tls_send_finished(void);
int net_tls_send_http_get(void);

#endif
